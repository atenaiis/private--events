<div class="card">
  <div class="card-header">
    Upcoming Events
  </div>
  <div class="card-body p-0">
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Title</th>
        <th>Date</th>
        <th>Location</th>
        <th>Description</th>
        <th colspan="2"></th>
      </tr>
    </thead>

    <tbody>
    
      <% @events.upcoming.each do |event| %>
        <tr>
          <td><%= event.title %></td>
          <td><%= event.date %></td>
          <td><%= event.local %></td>
          <td><%= event.description %></td>
          <td><%= link_to 'Show', event %></td>
          <%= link_to 'New Event', new_event_path, class: 'orange event_title fa fa-plus' %>
        </tr>
      <% end %>
    </tbody>
  </table>
  </div>
</div>

<hr>

<div class="card">
  <div class="card-header">
    Past Events
  </div>
  <div class="card-body p-0">
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Title</th>
        <th>Date</th>
        <th>Location</th>
        <th>Description</th>
        <th colspan="2"></th>
      </tr>
    </thead>

    <tbody>
    
      <% @events.previous.each do |event| %>
        <tr>
          <td><%= event.title %></td>
          <td><%= event.date %></td>
          <td><%= event.local %></td>
          <td><%= event.description %></td>
          <td><%= link_to 'Show', event %></td>
          <td></td>
          
        </tr>
      <% end %>
    </tbody>
  </table>
  </div>
</div>