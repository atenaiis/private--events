<div class="container-fluid ">
<h3 class="text-center">Attendance List</h3> <br><br>

<div class="row">

  <div class="col">

    <h4>User Data:</h4>

    <label>Name: </label>
    <h5><%= @user.name %></h5>

  </div>
  
  <div class="column">
        <p class="title has-text-dark has-text-centered">Created Events</p>
        <ul class="created-events-list">
          <% @events.each do |event| %>
            <li class="box has-text-centered">
              <%= link_to  event.title, event_path(event.id) %>
              <%= content_tag(:p, "#{event.date}  ") %>
            </li>
          <% end %>
        </ul>
      </div>
  
 
    <div class="col">
    <h4>Upcoming</h4>
    </div>
    <ul class="list-group col">
      <% @user.attended_events.upcoming.each do |event| %>
        <li class="list-group-item col"><tr>
        <td><%= event.title %></td>
        <label>Author: </label>
    <%= event.creator.name %>
        
       </tr></li>
       
       
      <% end %>
      </ul>
    <br>
    <div class="col">
    <h4>Attended</h4>
    </div>
    <ul class="list-group col">
    <% @user.attended_events.previous.each do |event| %>
      <li class="list-group-item col"><tr>
        <td><%= event.title %></td>
       </tr></li>
    <% end %>
    </ul>
    
  </div>
 </div>